# P2-1-S7 Fallback E2E 实现 plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement task-by-task.

**Goal:** pytest 级 E2E 验证 HybridRouter fallback 全链路正确：cloud 503 → 自动 local；local 死 + cloud 死 → 干净 echo；circuit 触发后短路。无 Docker / toxiproxy 依赖（用 httpx.MockTransport）。

**Architecture:** 在 `backend/tests/test_fallback_e2e.py` 用 FastAPI `TestClient` + WebSocket 客户端，monkeypatch 主 service_context 中 router 的 local/cloud provider 替换为 `OpenAICompatibleProvider` + `MockTransport`，构造 4 个端到端场景。

**Tech Stack:** pytest, fastapi.testclient, httpx.MockTransport, websockets。

**Spec:** `docs/superpowers/specs/2026-04-15-p2-1-finale-design.md` §2.3

**Branch:** `feat/p2-1-s7-fallback-e2e`

---

### Task 1: 探索现有 main.py 以决定 fixture 策略

**Files:**
- Read: `backend/main.py`
- Read: `backend/context.py`

- [ ] **Step 1: 验证可在 test fixture 内 swap service_context.llm_engine**

backend/main.py 在 module load 时构造 router. Tests 可以 import `service_context` 后改 `service_context._services["llm_engine"] = ...`. ServiceContext 设计应允许此覆盖。

如果 ServiceContext 不允许 overwrite（断言 register 只能一次），则需要小改 ServiceContext 加 `replace()` 方法 — 这是 minimal refactor，本任务允许。

读完后在本任务下记一行结论（"ServiceContext 允许 replace" / "需要加 replace 方法"）。

- [ ] **Step 2: 不需要 commit（探索任务）**

---

### Task 2: 编写 fallback E2E 测试骨架

**Files:**
- Create: `backend/tests/test_fallback_e2e.py`

- [ ] **Step 1: 写测试文件骨架**

```python
# backend/tests/test_fallback_e2e.py
"""P2-1-S7 fallback E2E.

Exercises the full chat path WITHOUT external network:
  TestClient(/ws/control) → agent_engine → HybridRouter → mocked providers.

Each scenario constructs a fresh HybridRouter with httpx.MockTransport-backed
OpenAICompatibleProvider instances, swaps it into service_context, and sends
a real `chat` message over the WebSocket.

This is the "real router code, fake network" test layer.
The S2 unit tests use _FakeProvider (no httpx involved); this suite uses
the real OpenAICompatibleProvider so SSE parsing + connection lifecycle
are also under test.
"""
from __future__ import annotations

import json
import pytest
import httpx
from fastapi.testclient import TestClient

from providers.openai_compatible import OpenAICompatibleProvider
from router.hybrid_router import HybridRouter
from agent.providers.simple_llm import SimpleLLMAgent


# --- helpers ---------------------------------------------------------------

SSE_OK = (
    b'data: {"choices":[{"delta":{"content":"hello "}}]}\n\n'
    b'data: {"choices":[{"delta":{"content":"world"}}]}\n\n'
    b'data: [DONE]\n\n'
)


def _ok_handler(req: httpx.Request) -> httpx.Response:
    if req.url.path.endswith("/models"):
        return httpx.Response(200, json={"data": [{"id": "fake-model"}]})
    return httpx.Response(200, content=SSE_OK,
                          headers={"content-type": "text/event-stream"})


def _503_handler(req: httpx.Request) -> httpx.Response:
    if req.url.path.endswith("/models"):
        # health probe still 200 so router will try chat — that's where 503 fires
        return httpx.Response(200, json={"data": [{"id": "fake-model"}]})
    return httpx.Response(503, text="upstream unavailable")


def _make_provider(handler, model="fake") -> OpenAICompatibleProvider:
    p = OpenAICompatibleProvider(
        base_url="http://invalid/v1", api_key="x", model=model,
    )
    p._test_transport = httpx.MockTransport(handler)
    return p


def _build_router_with(local_handler, cloud_handler) -> HybridRouter:
    local = _make_provider(local_handler, model="local-fake") if local_handler else None
    cloud = _make_provider(cloud_handler, model="cloud-fake") if cloud_handler else None
    return HybridRouter(local=local, cloud=cloud)


@pytest.fixture
def app_with_router(monkeypatch):
    """Build the real FastAPI app, then replace its llm_engine + agent_engine
    with versions wired to a mocked router. Yields (TestClient, set_router fn)."""
    import importlib
    import main as main_mod
    importlib.reload(main_mod)  # ensure fresh module each test
    monkeypatch.setattr(main_mod, "DEV_MODE", True)
    client = TestClient(main_mod.app)

    def set_router(r: HybridRouter):
        main_mod.service_context._services["llm_engine"] = r
        main_mod.service_context._services["agent_engine"] = SimpleLLMAgent(
            r, memory=main_mod.service_context.get("memory_store")
        )

    return client, set_router


def _send_chat_and_collect_response(client: TestClient, text: str) -> dict:
    with client.websocket_connect("/ws/control?secret=&session_id=fb-test") as ws:
        ws.send_json({"type": "chat", "payload": {"text": text}})
        while True:
            msg = ws.receive_json()
            if msg["type"] == "chat_response":
                return msg["payload"]
            # ignore pong / others


# --- scenarios -------------------------------------------------------------

def test_local_healthy_returns_local_response(app_with_router):
    """Baseline: both providers OK, local_first picks local."""
    client, set_router = app_with_router
    set_router(_build_router_with(_ok_handler, _ok_handler))
    payload = _send_chat_and_collect_response(client, "hi")
    assert "hello" in payload["text"]


def test_local_503_falls_back_to_cloud(app_with_router):
    """Local errors → router records failure → tries cloud → success."""
    client, set_router = app_with_router
    set_router(_build_router_with(_503_handler, _ok_handler))
    payload = _send_chat_and_collect_response(client, "hi")
    assert "hello" in payload["text"]  # cloud delivered


def test_local_503_and_cloud_503_returns_echo_fallback(app_with_router):
    """All providers dead → ws/control catches LLMUnavailableError → returns
    `[echo] hi` per existing main.py contract."""
    client, set_router = app_with_router
    set_router(_build_router_with(_503_handler, _503_handler))
    payload = _send_chat_and_collect_response(client, "hi")
    assert payload["text"] == "[echo] hi"


def test_cloud_only_when_local_unconfigured(app_with_router):
    """No local provider configured at all → goes straight to cloud."""
    client, set_router = app_with_router
    set_router(_build_router_with(None, _ok_handler))
    payload = _send_chat_and_collect_response(client, "hi")
    assert "hello" in payload["text"]


def test_circuit_opens_after_three_local_failures(app_with_router):
    """3 local failures → 4th request must skip local entirely.

    We can't directly observe 'didn't call' across handler boundary, but we
    can assert the response keeps coming from cloud after circuit opens.
    """
    client, set_router = app_with_router
    set_router(_build_router_with(_503_handler, _ok_handler))
    for _ in range(4):
        payload = _send_chat_and_collect_response(client, "x")
        assert "hello" in payload["text"]  # all four served by cloud
```

- [ ] **Step 2: Run — expect failures (router not swapped, missing reload, etc.)**

```bash
cd backend && DESKPET_DEV_MODE=1 uv run pytest tests/test_fallback_e2e.py -v
```

Iterate on fixture until all 5 PASS. Likely friction:
- `service_context._services` may be a different attr name → adapt
- `importlib.reload(main_mod)` may fail on repeated lifespan setup → use TestClient with custom app or a singleton context-replacement function

- [ ] **Step 3: 如有必要 — 在 ServiceContext 加 replace() 方法**

If `_services` access pattern is fragile, add to `backend/context.py`:

```python
class ServiceContext:
    ...
    def replace(self, name: str, value) -> None:
        """Test-only: swap a registered service. Production code uses register()."""
        self._services[name] = value
```

Then in fixture: `main_mod.service_context.replace("llm_engine", r)`.

- [ ] **Step 4: All 5 scenarios PASS**

```bash
cd backend && DESKPET_DEV_MODE=1 uv run pytest tests/test_fallback_e2e.py -v
```

Expect: 5 passed.

- [ ] **Step 5: Run full backend suite — no regression**

```bash
cd backend && uv run pytest -q
```

Expect: ≥ all prior tests pass + 5 new.

- [ ] **Step 6: Commit**

```bash
git add backend/tests/test_fallback_e2e.py backend/context.py
git commit -m "test(fallback): pytest E2E for HybridRouter fallback paths via MockTransport (P2-1-S7)"
```

---

### Task 3: 加一个 stress 场景（可选 — 高密度 fallback 不内存泄漏）

**Files:**
- Modify: `backend/tests/test_fallback_e2e.py`

- [ ] **Step 1: 加循环测试**

```python
def test_repeated_fallback_does_not_leak(app_with_router):
    """100 cycles of cloud-only after local-503 — assert no exception, no slow degradation."""
    client, set_router = app_with_router
    set_router(_build_router_with(_503_handler, _ok_handler))
    for _ in range(100):
        payload = _send_chat_and_collect_response(client, "x")
        assert "hello" in payload["text"]
```

- [ ] **Step 2: Run — should be < 30s**

```bash
cd backend && DESKPET_DEV_MODE=1 uv run pytest tests/test_fallback_e2e.py::test_repeated_fallback_does_not_leak -v --durations=5
```

- [ ] **Step 3: Commit**

```bash
git commit -am "test(fallback): add 100-cycle stress scenario (P2-1-S7)"
```

---

### Task 4: HANDOFF 文档

**Files:**
- Create: `docs/superpowers/handoffs/p2-1-s7-fallback-e2e.md`

包含：
- Goal / Commits / 5 (or 6) 测试场景列表 + 各自验证什么
- 决策记录：为什么不用 toxiproxy（无 Docker 依赖，CI 友好）
- 决策记录：为什么放 `backend/tests/` 而非 `tests/e2e/`（pytest fixture 模式 vs 真后端脚本）
- 后续 P2-2 实时双工时本套测试框架可复用
- Out of scope（CDP UI 层 E2E 留给 manual smoke）

```bash
git add docs/superpowers/handoffs/p2-1-s7-fallback-e2e.md
git commit -m "docs(p2-1-s7): handoff for fallback E2E slice"
```

---

## 完成判据

- [ ] `pytest backend/tests/test_fallback_e2e.py` 5–6 个场景全 PASS
- [ ] `pytest backend/` 整体不破坏现有
- [ ] handoff 已写
- [ ] 不引入 Docker / toxiproxy 等外部依赖
