---
description: "Test-Driven Development red-green-refactor — sp-test-driven-development skill 包装"
allowed-tools: ["Skill"]
---

User just invoked `/sp:tdd` with arguments: $ARGUMENTS

`$ARGUMENTS` is the feature / bug to TDD. Invoke the
`sp-test-driven-development` skill via the Skill tool. 它会强制 red →
green → refactor 循环，先写测试 + 让测试红 + 再写最小实现转绿。
