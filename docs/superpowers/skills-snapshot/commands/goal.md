---
description: "用 /goal codingsys 命令做长期目标管理 — sp-goal-management skill 包装"
allowed-tools: ["Skill"]
---

User just invoked `/sp:goal` with arguments: $ARGUMENTS

Invoke the `sp-goal-management` skill via the Skill tool. Skill 会教
怎么写硬证据 condition / ★ 一票否决用例 / 收敛标准 / 实机 vs 单测
的区别（v3 工具层 round1 → round2 教训提炼）。

`$ARGUMENTS` 如果是 goal condition 文本，skill 会按方法论 review +
建议改进版；如果空，skill 会给完整方法论 + 模板。
