# Local Dev Credentials — Template

> 这是模板。**复制为 `LOCAL-DEV-CREDENTIALS.md`**（已在 `.gitignore` 里）后填入你自己的凭据。
> 真实文件永远不会进 git。

## Relay 中转站登录账号（可选 — 仅 relay edition / 真 LLM E2E 测试用）

如果你要测 `npm run dev:relay` 构建（强制邮箱+密码登录中转站），需要：

1. 自己去 chinzy.com（或你自己搭的 token relay）注册账号
2. 在下面填入

```
邮箱：<your-email@example.com>
密码：<your-password>
```

**OSS 主线（manual edition）跑 `npm run dev` 不需要任何凭据** —— 启动后在
Settings → LLM Providers 里手填自己的 OpenAI / Anthropic / Ollama 即可。

## 用途

- 子代理 / windows-mcp E2E 测试走完整 LLM 链路时本地登录
- 验证 relay edition 的强制登录窗 + 账户面板
- 验证 chinzy 中转站 provider 自动配置链路

## 安全约束

- 本文件 **永远不要 commit**（`.gitignore` 已包含，但请二次确认）
- 截图 / diagnostic bundle 前关掉 onboarding 窗，避免账号密码被收集
- 测试代码请用 `monkeypatch.setattr("backend.secrets.get_cloud_api_key", lambda: "fake-sk-...")` mock，**不要硬编码凭据**
- 生产代码永远从 OS keychain 读
