# DeskPet 100 人内测 — 人工点击测试脚本

**配套**: [`2026-05-22-beta-100-readiness.md`](./2026-05-22-beta-100-readiness.md)
**用途**: 每个工作项实现完成后，由人（或 Claude 用 computer-use / windows-mcp）逐步实机点击验证。
**填写方式**: 每步执行后在 `通过?` 列填 ✅ / ❌；❌ 时在 `实际结果` 写现象 + 截图文件名。

---

## 测试环境准备

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| E1 | 准备一台干净 Windows 11 测试机（或快照），确认 `%AppData%\deskpet\` **不存在** | 目录不存在 | | |
| E2 | 准备一台"老用户"环境：已装过旧版、`%AppData%\deskpet\` 有 state.db + 旧 onboarding 标记 | 目录存在且有数据 | | |
| E3 | 准备一个**有效**的云 LLM `base_url` / `model` / `api_key`（测试用，额度够） | 凭据可用 | | |
| E4 | 准备一个**无效**的 api_key（用于测错误路径） | 凭据无效 | | |
| E5 | 任务管理器开着，准备核对进程 | — | | |
| E6 | 截图工具就绪，每个 ❌ 步骤都要截图 | — | | |

---

## 路径 A · 全新安装（干净机器）

### A0 · 安装与首次启动

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| A0-1 | 双击 MSI 安装包 | 安装向导启动 | | |
| A0-2 | 观察 SmartScreen | **WI-03 已签名**：显示发布者名，无红框 / **未签名**：出现「未识别的应用程序」（需在内测说明截图引导） | | |
| A0-3 | 完成安装 | 桌面 / 开始菜单有 DeskPet 图标 | | |
| A0-4 | 首次启动 DeskPet | StartupOverlay spinner「正在启动语音服务…」出现 | | |
| A0-5 | 等待 backend 起来 | spinner 消失，桌宠出现 | | |
| A0-6 | backend 启动失败时（可断网模拟）| StartupOverlay 切到失败态，有「重试 / 打开日志 / 退出」 | | |

### A1 · onboarding 向导（WI-01）

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| A1-1 | 首次启动后观察 | 自动弹出 onboarding 向导 Step 1 | | |
| A1-2 | 阅读 Step 1 欢迎页 | 有 DeskPet 简介 + 「下一步」「跳过」按钮 | | |
| A1-3 | 点「下一步」→ Step 2 | 出现 base_url / model / api_key 输入框 + 「测试连接」 | | |
| A1-4 | 留空直接点「下一步」 | 被拦截或「下一步」disabled，提示需先测试连接 | | |
| A1-5 | 填入 **E4 无效 key** → 点「测试连接」 | 明确错误提示（如「鉴权失败」），「下一步」仍 disabled | | |
| A1-6 | 改填 **E3 有效凭据** → 点「测试连接」 | 「连接成功」，「下一步」变 enabled | | |
| A1-7 | 点「下一步」→ Step 3 | 说明 BGE-M3 后台下载（286MB）、下载前记忆走 mock | | |
| A1-8 | 点「完成」 | 向导关闭，进入正常桌宠界面 | | |
| A1-9 | 检查 `%AppData%\deskpet\onboarding_done.json` | 文件存在，含版本号 + 完成时间 | | |
| A1-10 | 完全退出 DeskPet 再重启 | **不再**弹 onboarding 向导 | | |
| A1-11 | （另起干净环境）首次启动 → Step 1 直接点「跳过」 | 向导关闭；桌宠首条气泡提示"可在设置里配置大模型"；标记文件仍写入 | | |

### A2 · 基本对话冒烟

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| A2-1 | 对桌宠说一句话（文字输入） | 桌宠正常回复 | | |
| A2-2 | 连续对话 3 轮，引用前文 | 上下文连贯（known-issue #16 已修验证） | | |
| A2-3 | 语音输入一句话 | ASR 识别 + 回复（known-issue #13 验证） | | |
| A2-4 | 打开设置面板改 model → 保存 | **不报** `TypeError: Failed to fetch`（known-issue #1 验证）；保存成功 | | |

### A3 · 成本护栏（WI-04）

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| A3-1 | 设置面板查看「今日已用 X / Y 元」 | 显示实时用量数字 | | |
| A3-2 | 把 daily_budget 调到很小（如 0.01 元），多发几轮对话冲到 80% | 桌宠气泡提示「今日预算快用完了」（仅一次） | | |
| A3-3 | 继续对话冲到 100% | 对话被前置拦截，明确提示「已达每日预算上限」，**非静默失败** | | |
| A3-4 | 同一天再触发 80% 场景 | warning 不重复弹（每天一次） | | |
| A3-5 | 把 budget 设为 0 → 对话 | 不拦截，但用量数字仍累计 | | |
| A3-6 | 用 E4 无效 key 对话 | 错误明确区分"你的 key 有问题"而非"DeskPet bug" | | |

### A4 · 应用内反馈（WI-02）

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| A4-1 | Toolbar 找到「反馈」按钮 | 按钮可见可点 | | |
| A4-2 | 点开反馈面板 | 有文本框 + 「附带诊断包」复选框 + 「一键打包」按钮 | | |
| A4-3 | 文本框输入 < 10 字 | 「一键打包」disabled 或提示需更详细 | | |
| A4-4 | 输入 ≥ 10 字 → 点「一键打包诊断」 | 生成 zip，资源管理器高亮该文件，路径进剪贴板 | | |
| A4-5 | **解压 zip 逐个文件检查** | 含 crash_reports / 最近日志 / meta.json / metrics.jsonl | | |
| A4-6 | **打开 meta.json + 全文搜索** `api_key` / `secret` / `token` | **零命中**——绝不能有密钥 | | |
| A4-7 | 确认 zip 内无 `llm_runtime.json` 原文 | 不存在该文件 | | |
| A4-8 | 反馈面板查看收集渠道 | 群/邮箱/Issue 链接清晰可见 | | |

### A5 · 崩溃落盘 → 反馈链路（WI-02 + 既有 crash_reports）

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| A5-1 | 手动触发一次 backend 异常（如喂一个畸形输入 / 临时改配置制造启动失败） | `crash_reports/` 下出现新文件 | | |
| A5-2 | 触发一次 Rust 侧 panic（如可控的测试钩子） | `crash_reports/` 下出现 Rust 崩溃文件 | | |
| A5-3 | 再走 A4「一键打包」 | 新崩溃文件被收进诊断包 | | |

### A6 · feature-flag 默认态（WI-11）

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| A6-1 | 对照 `docs/beta-feature-flags.md` 逐项核对 config | memory-v2 五个 flag 实际为 OFF | | |
| A6-2 | 触发 ppt-generate skill（若内测放出）| 正常生成 .pptx / 或确认不可触发（若不放出） | | |
| A6-3 | 触发 deep-research skill（若内测放出）| 正常产出带引用报告 / 或确认不可触发 | | |
| A6-4 | 技能商店面板打开 | 不暴露半成品 / 内部 skill | | |

---

## 路径 B · 老用户升级

### B0 · 升级保数据

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| B0-1 | 在 E2 老用户环境装新版 MSI（覆盖安装） | 安装成功 | | |
| B0-2 | 启动新版 | **不弹** onboarding 向导（老用户标记被识别） | | |
| B0-3 | 检查历史对话 | state.db 旧对话还在，可召回 | | |
| B0-4 | 检查旧配置 | base_url / model / api_key 保留可用 | | |
| B0-5 | 旧 schema 残留场景（known-issue #12）| 启动不报 401 / 不请求废弃 URL | | |

### B1 · updater 端到端（WI-09）

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| B1-1 | 安装 `0.6.0-beta.1` | 版本号显示 beta.1 | | |
| B1-2 | 发布 `0.6.0-beta.2` 到 GitHub Release（含签名 latest.json） | Release 就绪 | | |
| B1-3 | beta.1 里点「检查更新」 | 检测到 beta.2，提示可更新 | | |
| B1-4 | 确认更新 | 下载 → 校验签名 → 替换 → 重启 | | |
| B1-5 | 重启后查看版本号 | 显示 beta.2 | | |
| B1-6 | 检查数据 | state.db / 配置 / onboarding 标记全部保留 | | |
| B1-7 | 断网点「检查更新」 | 优雅失败，应用不变砖 | | |
| B1-8 | 「检查更新」在已最新时 | 提示「当前已是最新」 | | |

---

## 路径 C · 进程生命周期（WI-05）

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| C-1 | 启动 DeskPet，任务管理器记下相关进程（deskpet.exe / python / 子进程） | 进程列表已记录 | | |
| C-2 | 点 ⏻ 退出按钮 | 5 秒内所有相关进程消失 | | |
| C-3 | 立即重启 DeskPet | 不报端口占用 / 不报 state.db 锁 | | |
| C-4 | 启动后用 Alt+F4 关闭 | 同样无残留 | | |
| C-5 | 启动后任务栏右键退出 | 同样无残留 | | |
| C-6 | 启动后直接注销 / 重启 Windows | backend 优雅关闭，重新登录后无僵尸 | | |
| C-7 | **循环「启动→⏻退出」共 10 次** | 每轮后 0 残留；`%AppData%\deskpet\` 下 `state.db.bak.*` ≤ 3 个 | | |
| C-8 | 制造 backend 崩溃 | StartupOverlay 进失败态，无僵尸 python 进程 | | |

---

## 路径 D · 卸载（WI-07）

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| D-1 | 控制面板 → 卸载 DeskPet | 卸载向导启动 | | |
| D-2 | （方案 A）出现「是否删除个人数据」复选框 | 复选框存在，文案清楚 | | |
| D-3 | 不勾选完成卸载 | 程序文件 / 开始菜单 / 自启动项移除；`%AppData%\deskpet\` 保留 | | |
| D-4 | 检查自启动 | 注册表 Run 项 / 计划任务**无残留** | | |
| D-5 | 卸载完成页 / 文档 | 明确写出残留数据路径 | | |
| D-6 | 重新安装 | 能识别旧 `%AppData%` 数据，正常启动、不弹 onboarding | | |
| D-7 | （勾选删除数据的场景）卸载 | `%AppData%\deskpet\` 被清空 | | |

---

## 路径 E · 文档核对（WI-06）

| # | 操作 | 预期结果 | 实际结果 | 通过? |
|---|---|---|---|---|
| E-1 | 找到内测协议文档 | 存在，含内测性质 / 保密 / 无 SLA | | |
| E-2 | 找到隐私说明 | 存在，说明 state.db 本地 / key 存凭据库 / 诊断包内容 | | |
| E-3 | **隐私说明逐条对代码核对** | 文档每句与实际行为一致（尤其"是否上传"） | | |
| E-4 | 找到用户版已知问题清单 | 存在，只含用户可见问题 + 规避方法 | | |
| E-5 | onboarding / about 面板能打开这三份 | 入口可达 | | |
| E-6 | WI-12 若做了遥测 → 隐私说明有写 | 一致；若没做遥测 → 写"无数据收集" | | |

---

## Go / No-Go 放行 Checklist

> 以下**全部 ✅** 才可放 100 人。任一 ❌ → No-Go，记录阻塞项。

| # | 放行项 | 状态 |
|---|---|---|
| G-1 | 路径 A 全新安装全部通过 | |
| G-2 | 路径 B 老用户升级全部通过 | |
| G-3 | 路径 C 进程生命周期全部通过 | |
| G-4 | 路径 D 卸载全部通过 | |
| G-5 | 路径 E 文档核对全部通过 | |
| G-6 | A4-6 诊断包**零密钥泄漏**（一票否决项）| |
| G-7 | A3-3 成本超额**明确拦截**（非静默）| |
| G-8 | MSI 已签名 ✅ / 或未签名但内测说明已含引导截图 + 风险已登记 | |
| G-9 | backend pytest ≥ 1635 passed，frontend vitest ≥ 242 passed，0 回归 | |
| G-10 | feature-flag 审计表 `docs/beta-feature-flags.md` 已产出 | |
| G-11 | `RELEASE.md` 发布流程可复现，回滚预案已写 | |
| G-12 | 5 人灰度观察 48h 无 P0 崩溃 | |

**放行决定**: ☐ Go　☐ No-Go
**决定人 / 日期**:
**遗留风险登记**:

---

## 执行说明（给 Claude）

计划全部实现完成后，由 Claude 执行本脚本：
1. 用 `windows-mcp` / `computer-use` 在测试机实机操作。
2. 逐行填 `实际结果` + `通过?`。
3. 每个 ❌ 步骤截图，文件名记入 `实际结果` 列。
4. 跑完填写末尾 Go/No-Go 决定。
5. 输出一份测试报告小结（通过率 / 阻塞项 / 建议）。
