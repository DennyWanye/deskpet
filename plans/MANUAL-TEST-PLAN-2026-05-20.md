# DeskPet 人工测试用例 — 2026-05-20

> **覆盖范围**：本会话所有 commit + 新增 AuthAdapter scaffold
> **执行人**：真人 / 自动化（computer-use / Playwright）
> **预计时长**：30-45 分钟
> **当前分支**：`feat/2026-05-18-session-and-e2e`
> **依赖**：dev 栈在跑（`npm run tauri dev` + 桌宠已连接、绿色 ✓「已连接」徽章）

---

## 涉及 Commit 一览

| Commit | 范围 |
|---|---|
| `a0d11a4` | toggle、DialogBar 隐藏、warnings/errors、模型切换、面板发送一致性 |
| `9f9d932` | capability 修复（拖动 / 全屏权限）+ InputBar sessionId="default" |
| `19ff75f` | 面板宽度 360→440 + mic 语音按钮 |
| 当前未提交 | 主界面输入栏在面板打开时隐藏 + AuthAdapter scaffold |

## 前置条件

1. **进程清洁**：先确认无僵尸 deskpet/python 进程
   ```powershell
   Get-Process deskpet,deskpet-backend -EA SilentlyContinue
   ```
2. **栈启动**（如未跑）：
   ```powershell
   $env:DESKPET_PYTHON="G:\projects\deskpet\backend\.venv\Scripts\python.exe"
   $env:DESKPET_BACKEND_DIR="G:\projects\deskpet\backend"
   $env:DESKPET_DEV_ROOT="G:\projects\deskpet"
   Set-Location G:\projects\deskpet\tauri-app; npm run tauri dev
   ```
3. **就绪信号**：右上 Toolbar 显示 `已连接`（绿底） + `FPS` 大于 0
4. **首次启动状态**：面板隐藏，桌宠在右下角，桌宠底部输入栏可见

---

## TC 组别索引

| 组 | 主题 | 用例数 | 阻塞级别 |
|---|---|---|---|
| TC-01 | 面板 toggle 与可见性 | 4 | P0 |
| TC-02 | 桌宠 DialogBar 与底部输入栏隐藏 | 4 | P0 |
| TC-03 | 消息流面板（filter tabs） | 4 | P1 |
| TC-04 | 面板发送一致性（sessionId="default"） | 5 | P0 |
| TC-05 | 面板拖动（startDragging） | 4 | P1 |
| TC-06 | 面板全屏（setFullscreen） | 4 | P1 |
| TC-07 | 面板宽度 + 模型芯片不破版 | 4 | P1 |
| TC-08 | 面板模型 + 参数切换 | 6 | P0 |
| TC-09 | 面板 mic 语音按钮 | 6 | P0 |
| TC-10 | 主界面输入栏关联隐藏 | 3 | P0 |
| TC-11 | AuthAdapter scaffold 零回归 | 4 | P0 |
| TC-12 | 既有功能回归 smoke | 5 | P0 |

**P0 = 阻塞合并；P1 = 体验问题但不阻塞。**

---

## TC-01 · 面板 toggle 与可见性

### TC-01.1 点击 ▶ 消息打开面板
**步骤**：点击桌宠窗口左侧的「▶ 消息」按钮
**预期**：左侧出现深色玻璃质感面板，docked 在桌宠正左方；header 显示「💬 消息 · 主线程」+ 模型芯片 +「⛶」
**截图建议**：是

### TC-01.2 再次点击 ▶ 消息关闭面板
**步骤**：面板打开状态下，再次点击桌宠的「▶ 消息」
**预期**：面板从屏幕上消失；桌宠位置不变；右下 DialogBar 与底部输入栏恢复（见 TC-02）

### TC-01.3 点击面板内的 ◀ 关闭
**步骤**：面板打开状态下，点击面板 header 左侧的「◀」按钮
**预期**：与 TC-01.2 相同

### TC-01.4 toggle 反复 5 次无残影 / 无闪烁
**步骤**：连续点击「▶ 消息」5 次
**预期**：每次 toggle 干净，无窗口残影、无白闪、桌宠未跳位

---

## TC-02 · 桌宠 DialogBar 与底部输入栏隐藏

### TC-02.1 面板打开 → 桌宠 DialogBar 不可见
**步骤**：发送一条消息让桌宠有 latestAssistant 内容（如「你好」），然后打开面板
**预期**：桌宠角色头顶 / 旁边的对话气泡（DialogBar）消失

### TC-02.2 面板打开 → 桌宠底部输入栏不可见 ★ 本会话新增
**步骤**：打开面板
**预期**：桌宠窗口底部的「🎤 mic + 文字输入框 + 发送按钮」整条**消失**

### TC-02.3 面板关闭 → 桌宠 DialogBar 恢复
**步骤**：从 TC-02.1 状态关闭面板
**预期**：DialogBar 重新出现（若仍有 latestAssistant 内容）

### TC-02.4 面板关闭 → 桌宠底部输入栏恢复 ★
**步骤**：从 TC-02.2 状态关闭面板
**预期**：mic+输入+发送条立即重新出现，位置与打开前一致

---

## TC-03 · 消息流面板（filter tabs）

### TC-03.1 默认显示「全部」tab
**步骤**：打开面板
**预期**：「全部」高亮（绿色或蓝色背景），其余三个 tab 默认无背景

### TC-03.2 「对话」tab
**步骤**：点击「对话」
**预期**：只显示 user / assistant chat 气泡，无 warning / error 卡片

### TC-03.3 「⚠ 警告」tab
**步骤**：点击「⚠」（橙色）
**预期**：只显示警告类条目；无警告时显示空状态文案

### TC-03.4 「🚨 错误」tab
**步骤**：点击「🚨」（红色）
**预期**：只显示错误类条目；无错误时显示空状态文案

---

## TC-04 · 面板发送一致性 ★ 核心

### TC-04.1 输入框输入字符
**步骤**：在面板底部输入框敲「测试 1234」
**预期**：字符逐个进入；placeholder「和桌宠说点什么…」消失

### TC-04.2 发送按钮变红 + 状态变思考中
**步骤**：点击「发送」（或按 Enter）
**预期**：
- 输入框清空
- 「发送」按钮变红色「■ 停止」
- 左下角状态从「✓ 空闲」变「⏳ 思考中」

### TC-04.3 回复到达面板 default 流
**步骤**：等待 5-15s
**预期**：
- 助手回复出现在面板消息流末尾（可能需要滚动）
- 状态变回「✓ 空闲」
- 按钮变回「发送」

### TC-04.4 桌宠 DialogBar 不显示（因为面板打开）
**步骤**：TC-04.3 完成后观察桌宠
**预期**：桌宠 DialogBar 仍隐藏（与 TC-02.1 一致）

### TC-04.5 关闭面板，主输入框发消息也进 default 流
**步骤**：
1. 关闭面板
2. 在桌宠主输入框发「主界面测试」
3. 等回复完成
4. 再打开面板
**预期**：面板消息流的 default 末尾应包含「主界面测试」+ 回复（共享同一会话）

---

## TC-05 · 面板拖动（startDragging）

> **注意**：synthetic 鼠标无法驱动 Windows OS 模态拖动循环，必须**真人物理鼠标**

### TC-05.1 标题栏空白拖动
**步骤**：物理鼠标按住面板 header 的空白区域（「💬 消息 · 主线程」文字附近）拖动到屏幕中央
**预期**：整个面板窗口跟随鼠标移动

### TC-05.2 拖动到屏幕左上
**步骤**：从 TC-05.1 位置拖到屏幕左上 (200, 200)
**预期**：面板移到那里，桌宠主窗口位置不变

### TC-05.3 拖动后保持在新位置
**步骤**：松开鼠标
**预期**：面板停在松开位置，**不会**自动 re-dock 到桌宠左侧

### TC-05.4 拖动按钮不触发拖动
**步骤**：尝试按住「◀」/「⛶」/ 模型芯片这些 header 内的按钮拖动
**预期**：拖动**不触发**（这些按钮 onMouseDown stopPropagation），单击行为正常

---

## TC-06 · 面板全屏（setFullscreen）

### TC-06.1 点击 ⛶ 进入全屏
**步骤**：点击 header 右上「⛶」
**预期**：面板瞬间铺满整个屏幕；任务栏可能被覆盖（取决于显示器配置）

### TC-06.2 全屏下所有元素可见
**步骤**：在 TC-06.1 状态观察
**预期**：
- header 横跨顶部
- 消息流填满中间
- 输入栏 + mic + 发送在底部
- 「⛶」按钮在右上角可见

### TC-06.3 再次点击 ⛶ 退出全屏
**步骤**：在全屏状态下点击「⛶」
**预期**：回到 docked 尺寸 + 位置（440x600，贴桌宠左侧）

### TC-06.4 拖动后再全屏 → 还原回拖动位置
**步骤**：
1. 拖动面板到屏幕中央
2. 进全屏
3. 退全屏
**预期**：面板回到中央拖动位置（不是 docked 位置）

---

## TC-07 · 面板宽度 + 模型芯片不破版

### TC-07.1 默认模型芯片
**步骤**：观察 header 模型芯片
**预期**：显示「默认模型 ✎」，标题「消息·主线程」**横排单行**

### TC-07.2 切换到长名称模型
**步骤**：
1. 点击模型芯片
2. 选择 `deepseek-v4-pro` 或 `claude-opus-4.5` 等长名字
3. 保存
**预期**：芯片更新为长名 + ✎ 编辑图标

### TC-07.3 切换后标题不破版 ★
**步骤**：观察 TC-07.2 后的 header
**预期**：标题**不应**出现「消\n息\n·\n主\n线\n程」竖排逐字 wrap

### TC-07.4 440px 宽下 4 个元素同行
**步骤**：观察 header
**预期**：◀ + 💬 标题 + 模型芯片 + ⛶ 在同一行内，无溢出 / 截断

---

## TC-08 · 面板模型 + 参数切换

### TC-08.1 点击模型芯片打开 modal
**步骤**：点击 header 模型芯片
**预期**：ChangeModelModal 弹出，覆盖在面板上层

### TC-08.2 OpenAI 系列显示 reasoning_effort
**步骤**：选 `gpt-5.2` / `gpt-4o` 等 OpenAI 模型
**预期**：参数区出现「思考强度」滑块（low / medium / high）

### TC-08.3 Claude 系列显示 thinking 不显示 effort
**步骤**：选 `claude-opus-4.5` / `claude-3.5-sonnet`
**预期**：参数区出现「Thinking」开关 + budget_tokens 输入；**不显示** reasoning_effort

### TC-08.4 DeepSeek 显示 thinking
**步骤**：选 `deepseek-v3.2` 或类似
**预期**：与 TC-08.3 一致

### TC-08.5 保存后芯片更新
**步骤**：保存
**预期**：modal 关闭，模型芯片立即显示新选择的模型名

### TC-08.6 发消息使用新模型
**步骤**：保存后在面板发一句「你的模型是什么」
**预期**：助手回复中提到与所选一致的模型名（可能上游会自报家门）

---

## TC-09 · 面板 mic 语音按钮 ★ 新增

### TC-09.1 mic 按钮渲染
**步骤**：打开面板
**预期**：输入框左侧出现圆形按钮（36×36），含 🎤 emoji

### TC-09.2 初始颜色 = 连接态
**步骤**：观察 mic 按钮颜色
**预期**：靛蓝色背景（`rgba(99,102,241,0.30)`），表示 audio channel 已连接

### TC-09.3 点击进入录音态
**步骤**：点击 mic 按钮
**预期**：
- 首次可能弹 WebView2 麦克风权限对话框 → 允许
- 按钮立刻变红色背景
- 图标变 ⏹

### TC-09.4 录音 pulse 动画
**步骤**：在 TC-09.3 状态观察
**预期**：按钮有 pulse（缩放 1.0 ↔ 1.1，opacity 1.0 ↔ 0.7）的循环动画

### TC-09.5 再次点击退出录音
**步骤**：点击 ⏹
**预期**：背景变回靛蓝，图标回 🎤，动画停止

### TC-09.6 真实说话 → 转写入面板 default 流
**步骤**：
1. 点击 mic 进入录音
2. 对麦克风说「你好你叫什么名字」
3. 等待 VAD 检测到语音结束（约 1s 静音）
4. 等回复完成
**预期**：
- 用户转写「你好你叫什么名字」出现在面板消息流（蓝色 user 气泡）
- 助手回复出现在下面（assistant 气泡）
- 同时 TTS 通过桌宠或面板的音频通道播放
- 桌宠 Live2D 嘴型同步动（lip_sync 仍走桌宠 control_ws）

---

## TC-10 · 主界面输入栏关联隐藏 ★ 本次新加

### TC-10.1 面板未打开时主输入栏可见可用
**步骤**：关闭面板，观察桌宠
**预期**：桌宠底部圆角胶囊形 mic + 输入 + 发送 条**可见且可点**

### TC-10.2 面板打开瞬间主输入栏消失
**步骤**：点击「▶ 消息」打开面板
**预期**：桌宠底部输入条**立即**消失，无残影、无动画

### TC-10.3 面板关闭瞬间主输入栏恢复
**步骤**：关闭面板
**预期**：桌宠底部输入条**立即**回来，与 TC-10.1 完全一致（位置 / 样式 / 可用性）

---

## TC-11 · AuthAdapter scaffold 零回归 ★ 本次新加

### TC-11.1 tsc 通过
**步骤**：`cd tauri-app && npx tsc --noEmit; echo $?`
**预期**：EXIT 0，无错误输出

### TC-11.2 vitest 全部通过
**步骤**：`npx vitest run`
**预期**：
- 12 test files passed（原 11 + 新增 `auth.test.ts`）
- 173 tests passed（原 154 + 新增 19 个 auth 单测）

### TC-11.3 既有 UI 行为零变化
**步骤**：TC-01 到 TC-10 全部跑通
**预期**：所有现有功能不受 auth 改动影响（因为没有 wire 到现有组件）

### TC-11.4 可以从外部 import 新模块
**步骤**：在浏览器开发者控制台或 vite preview 验证：
```ts
import { getAuthAdapter, NullAuthAdapter, ManualAuthAdapter } from "./auth";
const a = getAuthAdapter();
console.log(a.id, a.displayName, a.isAuthenticated());
```
**预期**：log 输出 `manual 自托管（手填 provider） true`

---

## TC-12 · 既有功能回归 smoke

### TC-12.1 桌宠主输入框可发消息且回复正常
**步骤**：关闭面板，主输入框发「ping」
**预期**：桌宠 DialogBar 显示回复气泡，无 console error

### TC-12.2 桌宠主 mic 按钮可正常工作
**步骤**：关闭面板，点击桌宠主 mic 按钮 → 录音 → 说话 → 停
**预期**：与 TC-09 类似流程，但用桌宠主窗口的 mic（不在面板里）

### TC-12.3 Toolbar 所有按钮可点
**步骤**：依次点击右上角 Toolbar 的「📁」「🎬」「⚙」「⏻」等
**预期**：每个按钮触发对应面板 / 菜单，无 crash

### TC-12.4 Code Panel（如有入口）不受 sessionId prop 影响
**步骤**：如果项目有 Code Panel 入口，打开 Code Panel
**预期**：Code Panel 的 InputBar 行为不变（不传 sessionId → 用 active_sid 路径）；发消息正常 echo 到 active session

### TC-12.5 重启栈后状态持久化
**步骤**：
1. 关闭整个 Tauri 应用
2. 重新 `npm run tauri dev`
3. 打开面板
**预期**：preferred_model + 历史消息正确恢复（来自 SessionDB）

---

## Pass / Fail 记录

| TC | 状态 | 截图 / 备注 |
|---|---|---|
| TC-01.1 | ⬜ | |
| TC-01.2 | ⬜ | |
| TC-01.3 | ⬜ | |
| TC-01.4 | ⬜ | |
| TC-02.1 | ⬜ | |
| TC-02.2 | ⬜ | |
| TC-02.3 | ⬜ | |
| TC-02.4 | ⬜ | |
| TC-03.1 | ⬜ | |
| TC-03.2 | ⬜ | |
| TC-03.3 | ⬜ | |
| TC-03.4 | ⬜ | |
| TC-04.1 | ⬜ | |
| TC-04.2 | ⬜ | |
| TC-04.3 | ⬜ | |
| TC-04.4 | ⬜ | |
| TC-04.5 | ⬜ | |
| TC-05.1 | ⬜ | 真人物理鼠标 |
| TC-05.2 | ⬜ | 真人物理鼠标 |
| TC-05.3 | ⬜ | 真人物理鼠标 |
| TC-05.4 | ⬜ | 真人物理鼠标 |
| TC-06.1 | ⬜ | |
| TC-06.2 | ⬜ | |
| TC-06.3 | ⬜ | |
| TC-06.4 | ⬜ | |
| TC-07.1 | ⬜ | |
| TC-07.2 | ⬜ | |
| TC-07.3 | ⬜ | ★ 重点 |
| TC-07.4 | ⬜ | |
| TC-08.1 | ⬜ | |
| TC-08.2 | ⬜ | |
| TC-08.3 | ⬜ | |
| TC-08.4 | ⬜ | |
| TC-08.5 | ⬜ | |
| TC-08.6 | ⬜ | |
| TC-09.1 | ⬜ | |
| TC-09.2 | ⬜ | |
| TC-09.3 | ⬜ | 需真实麦克风 |
| TC-09.4 | ⬜ | |
| TC-09.5 | ⬜ | |
| TC-09.6 | ⬜ | 需真实麦克风 |
| TC-10.1 | ⬜ | ★ 重点 |
| TC-10.2 | ⬜ | ★ 重点 |
| TC-10.3 | ⬜ | ★ 重点 |
| TC-11.1 | ✅ | tsc EXIT 0（已自动跑） |
| TC-11.2 | ✅ | vitest 173/173（已自动跑） |
| TC-11.3 | ⬜ | 跟着 TC-01~10 跑就够 |
| TC-11.4 | ⬜ | DevTools 验证 |
| TC-12.1 | ⬜ | |
| TC-12.2 | ⬜ | |
| TC-12.3 | ⬜ | |
| TC-12.4 | ⬜ | |
| TC-12.5 | ⬜ | |

---

## Computer-Use 自动化说明

**当前会话状态**：computer-use MCP **已断开**（系统提示 27 个工具不可用），无法执行最后一步「合成点击 + 输入」E2E 模拟。

**如 computer-use 后续恢复**，可参考以下坐标基线（基于 1920×1080 显示器，桌宠默认位置 x≈2140 但本机 1456 宽屏下约为 1200-1456）：

| 元素 | 大致坐标 |
|---|---|
| 桌宠「▶ 消息」tab | (1218, 535) |
| 面板 header「⛶」全屏 | (1190, 389) |
| 面板模型芯片 | (1142, 389) |
| 面板输入框中心 | (1080, 645) |
| 面板「发送」按钮 | (1173, 661) |
| 面板 mic 按钮 | (993, 661) |
| 面板「◀」关闭 | (1033, 389) |
| 桌宠主输入框 | (1335, 679) |
| 桌宠主 mic 按钮 | (1248, 679) |

**真实物理交互必须项**：TC-05（拖动）和 TC-09.3/9.6（真麦克风）即使 computer-use 恢复也**不能**用合成输入验证 —— Windows OS 模态拖动循环和真实音频流需要硬件层输入。

---

## 退出条件

- 所有 P0 用例 pass → 可以合并 / 发版
- 任何 P0 fail → 阻塞，回滚或修复
- P1 fail → 列入下一迭代 backlog，不阻塞

---

## 变更历史

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-05-20 | v1.0 | 初版，覆盖本会话 3 个 commit + AuthAdapter scaffold |
