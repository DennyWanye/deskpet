# T6.3 — computer-use real E2E (code-session-model-params)

Date: 2026-05-19
Branch: `feat/2026-05-18-session-and-e2e`
Stack: single clean dev stack (`npm run tauri dev`, `.venv` backend),
backend `Uvicorn running on http://127.0.0.1:8100`, migration 008
applied at runtime (`state.db user_version=16`, `code_session_provider.model_params`
column present).

## Procedure (real UI, computer-use)

1. Opened DeskPet → **Code Mode** (多项目仪表盘) → session **小说网站**
   (`base_session_id=code-rkjdd9vo`, `G:\projects\小说网站`).
2. Provider dropdown → **✏️ 改 model...** → the new Cursor-style
   **"模型与参数"** picker opened (screenshot evidence).
3. In the picker set: **模型 = GPT-5.5**, **Thinking = ON**,
   **Fast = off**, **上下文窗口 = 300K**, **推理强度(Effort) = High** → 保存.
4. Session tile immediately showed the **`gpt-5.5`** badge.
5. Sent a code task to that session
   ("用一句话说明这个项目是做什么的，不要写代码不要调用工具") →
   got a coherent assistant reply (real LLM call completed).

Screenshots:
- `T6.3-e2e-primary.png` — primary display: Code Mode dashboard,
  小说网站 tile with `gpt-5.5` badge + the task + AI reply.
- `T6.3-e2e-picker-gpt5.5.png` — full virtual-screen capture.

## Definitive proof (live persisted data, replicates `main.py`)

Persisted binding row written by the picker via the IPC contract
(`code_session_set_model {session_id, model, params}` → SessionDB,
migration-008 `model_params` column):

```
code_session_provider:
('code-rkjdd9vo', None, 'gpt-5.5',
 '{"thinking": true, "fast": false, "context": "300k", "effort": "high"}')
```

Running the exact `resolve_provider_for_session(...)` path `main.py`
uses, against the live `state.db` + the chinzy chain
(`deepseek-v4-pro`), with `code_default_model='gpt-5.5'`:

```
binding(code-rkjdd9vo) = {'provider_id': None,
  'preferred_model': 'gpt-5.5',
  'model_params': {'thinking': True, 'fast': False,
                   'context': '300k', 'effort': 'high'}}
mapper(model_params) -> {'reasoning_effort': 'high',
                         'extra_body': {'context_window': 300000}}

CODE  session (code-rkjdd9vo) -> model='gpt-5.5'
       code_params={'reasoning_effort': 'high',
                    'extra_body': {'context_window': 300000}}
PET   default               -> model='deepseek-v4-pro'
       code_params={}
```

### Result — PASS

- The Cursor-style picker → IPC → SessionDB round-trip persisted the
  exact structured params (thinking/fast/context/effort).
- The code session's next agent call resolves **`gpt-5.5`** with
  **`reasoning_effort=high`** + `context_window=300000` (300K),
  exactly what the picker selected (effort `high` passes through; the
  UI's Extra High/Max would clamp to `high` server-side).
- The pet / companion **`default`** session is **provably untouched** —
  resolves `deepseek-v4-pro`, `code_params={}` (no leakage from the
  code-session binding into the companion path).
- Backend startup log independently confirms the pet/companion runtime
  is chinzy `deepseek-v4-pro`
  (`llm_runtime_overrides_applied ('https://chinzy.com/v1','deepseek-v4-pro')`,
  `supervisor_provider_resolved ('chinzy','https://chinzy.com/v1','deepseek-v4-pro')`).

## Regression gate (T6.1)

- backend: `1465 passed, 10 skipped, 0 failed`
  (`--ignore=tests/test_deskpet_vector_worker.py`, project convention).
- frontend: `npx tsc --noEmit` EXIT=0; full vitest **147/147**
  (+10 new picker/IPC/ws tests). 0 regressions.
