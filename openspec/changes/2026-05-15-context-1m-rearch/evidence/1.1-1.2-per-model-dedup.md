# Evidence: 1.1-1.2 per-model context map + file-read dedup

**When**: 2026-05-15 17:08:42 UTC+8
**Who**: subagent (Phase 1.1 + 1.2, coalesced — both own context_manager.py)
**What we tested**: per-model 三层 context window 解析 + ContextConfig 比例阈值 +
CodeModeManager/main.py 注入 + 模型上下文 IPC 卡片 + 同 path read dedup —— 全部
通过严格 TDD（先红后绿）单测覆盖。Live E2E 由 lead agent 做（任务约定）。

## Scope delivered

### Phase 1.1 — per-model context map + 三层配置
- **NEW** `backend/llm/model_info.py`：`ModelContextInfo`（frozen dataclass）+
  `BUILTIN` 内置表（design.md D1 数值）+ `load_global_overrides()` +
  `load_project_overrides(root)` + `resolve(model, root)` 纯函数，三层深合并
  （builtin ← %APPDATA%/deskpet/model_overrides.toml ← <root>/.deskpet/context.toml）
- **MODIFIED** `backend/agent/context_manager.py::ContextConfig`：注入
  `model_info` + `v2_enabled`；阈值改 `@property` 按 window 比例算
  （compact_at_tokens / tool_result_threshold / compact_message_threshold /
  compact_char_threshold / effective_window_tokens）；`v2_enabled=false` 回退
  2026-05-15 stop-gap 绝对值（Strangler-Fig）
- **NEW** `ContextManager.for_session(model, project_root, v2_enabled)` 工厂
- **MODIFIED** `backend/main.py`（仅 ContextManager 实例化处 ~3050）：按
  `_provider.model` + code-mode `project_root` + `[context.manager].v2_enabled`
  调 `for_session`；lifespan 启动落一行 `model_context_resolved`
- **MODIFIED** `config.toml`：新增 `[context.manager] v2_enabled = true` 回退闸
- **NEW** `backend/p4_ipc.py`：`model_context_get` / `model_context_set` handler
  （手写 TOML 深合并写回 global/project，无 tomli_w 依赖）
- **NEW** `tauri-app/src/components/ModelContextCard.tsx` + 接入 SettingsPanel
  「模型状态」段；`tauri-app/src/types/messages.ts` 加 4 个类型 + 入 union

### Phase 1.2 — file-read dedup（design.md D3）
- **MODIFIED** `backend/agent/context_manager.py`：`ContextConfig.read_class_tools`
  白名单 + `ContextManager._read_path_seen: dict[str,int]` +
  `dedup_file_reads(messages, tool_name, tool_args, new_index, iteration)`：
  同 path 重复 read → 历史里旧 tool_result content 原地替换成
  `<file X was re-read at iteration N; superseded — see the later read>`；
  不删消息（message 数组长度/下标稳定）；path 规范化 `Path.resolve().casefold()`
  （Windows 大小写盘符 / 正反斜杠归一）；write/edit/run_shell 不去重

> 注：`dedup_file_reads` 的 agent_loop 接线点不在本 subagent 允许文件清单内
> （agent_loop.py 非 scope），方法契约 + 单测已就绪，lead agent 负责接线 + 真 E2E
> （任务明确："真 E2E 由 lead agent 做，你只做单测"）。

## Steps

1. 严格 TDD：每个 (test, code) 对先写失败测试 → 跑 pytest 见红 → 最小实现转绿
2. reconcile：db20b25 上 `test_p6_context_manager.py` 已有 7 条 + `test_p6_integration.py`
   1 条 **pre-existing 失败**（测的是被 proposal 明确"取代"的 2026-05-15
   stop-gap 绝对值）；按 spec 语义更新这些 legacy 测试到 v2 + 保留 v1 回退闸覆盖
3. 跑两条 verify 命令 + 前端 tsc/vitest

## Observation

### 测试新增（file::test）
- `test_model_info.py`：11 tests（builtin 表 / _default 兜底 / global 深合并 /
  project>global>builtin / 非 code mode 跳项目层 / resolve 日志 / frozen 值对象）
- `test_context_config_v2.py`：8 tests（compact_at 随 window 伸缩 1M→750K /
  200K→166K / tool_result max(8K,window//25) / v1 legacy 回退）
- `test_context_manager_for_session.py`：6 tests（builtin / 缺失 model /
  项目覆盖 code mode / v2 off legacy / 自定义 ref_store / resolve 日志）
- `test_model_context_ipc.py`：9 tests（get builtin/global / set 写 global/
  project TOML 深合并不抹其他 model / project scope 需 root / 拒非白名单字段 /
  类型注册）
- `test_context_manager_dedup.py`：8 tests（reread supersede / 3 读只留最后 /
  Windows 大小写 / 正反斜杠 / write·exec 不去重 / mcp_read 是 read-class /
  无 path noop / 返回 supersede 计数）
- `ModelContextCard.test.tsx`：8 tests（get/set 线格式 / parse 白名单+范围）
- 更新 legacy：`test_p6_context_manager.py`、`test_p6_integration.py`、
  `test_deskpet_p4_ipc.py`（membership guard 6→8）对齐 v2 语义

### Verify 命令 1（required tests）尾 10 行
```
tests/test_context_manager_dedup.py::test_reread_supersedes_earlier_copy PASSED [ 63%]
tests/test_context_manager_dedup.py::test_three_reads_only_last_kept PASSED [ 68%]
tests/test_context_manager_dedup.py::test_path_normalization_windows_casing PASSED [ 73%]
tests/test_context_manager_dedup.py::test_path_normalization_forward_back_slash_same PASSED [ 78%]
tests/test_context_manager_dedup.py::test_write_exec_tools_not_deduplicated PASSED [ 84%]
tests/test_context_manager_dedup.py::test_mcp_filesystem_read_is_read_class PASSED [ 89%]
tests/test_context_manager_dedup.py::test_no_path_arg_is_noop PASSED     [ 94%]
tests/test_context_manager_dedup.py::test_dedup_returns_superseded_count PASSED [100%]

============================= 19 passed in 0.16s ==============================
```

### Verify 命令 2（full suite, no regressions）尾 8 行
```
........................................................................ [ 72%]
........................................................................ [ 78%]
........................................................................ [ 83%]
........................................................................ [ 89%]
........................................................................ [ 94%]
.................................................................s       [100%]
1277 passed, 13 skipped, 4 deselected in 79.95s (0:01:19)
```

### 前端
```
tsc --noEmit -p tsconfig.json  → 0 errors（clean）
vitest src/components/ModelContextCard.test.tsx → 8 passed
vitest 全套 → 10 files / 117 tests passed（含新增 ModelContextCard）
```

## Conclusion

- ✅ Phase 1.1 spec `per-model-context` 全 7 场景单测覆盖通过
- ✅ Phase 1.2 spec `long-run-context` "File-read deduplication" Requirement
  全 3 场景单测覆盖通过（SHA-256 / Checkpoint-Restart 是 Phase 2，非本 scope）
- ✅ verify 命令 1：19 passed（test_model_info 11 + test_context_manager_dedup 8）
- ✅ verify 命令 2：1277 passed / 0 failed，零回归
- ✅ `v2_enabled=false` Strangler-Fig 回退闸保留旧绝对值路径（spec 场景覆盖）
- Deviations:
  - db20b25 基线上 `test_p6_context_manager.py`(7) + `test_p6_integration.py`(1)
    本就 RED（测的是 proposal 明确"被取代"的 stop-gap 绝对值）。按 spec 语义
    更新这些 legacy 测试至 v2 + 新增 v1 回退闸覆盖——这些现在全绿，是修复非破坏。
  - `test_p6_context_manager.py::test_three_reads_only_last_kept` 我初版断言过严
    （要求每个 marker 都指最后一次 iteration）；spec 只要求"keeping only the
    most recent read"。实现让 marker 指"被哪次读顶掉的"（更可追溯），同样满足
    spec——修正的是测试断言不是实现。
- Followup（flag 给 lead）:
  - `dedup_file_reads` 需 lead 在 `agent_loop.py` tool message append 后接线
    （传 `tc.arguments` + `new_index` + `iteration`）才在生产生效；非本 scope
  - `backend/agent/token_budget.py::_KNOWN_WINDOWS` 仍硬编码 deepseek=64_000，
    `check_budget` 没读 model_info。本 scope 只改 ContextConfig（D2）；budget
    分母 per-model 化建议后续 slice 跟进（token_budget.py 非本 scope 允许文件）
  - 为跑前端 tsc/vitest 在 worktree `tauri-app/` 建了 `node_modules` junction
    指向主仓（worktree 无 node_modules）——非破坏性 dev-only，lead 合并时注意
    这是 junction 不是真目录（git 不会跟踪）
```
